@model DAWProject.Models.Division
@{
    ViewBag.Name = "Update";
}

<h2>@ViewBag.Name</h2>

@using (Html.BeginForm(actionName: "Edit", controllerName: "Division"))
{
    @Html.HttpMethodOverride(HttpVerbs.Put)

    @Html.HiddenFor(b => b.DivisionId)
    <br />
    <br />

    @Html.Label("Name", "Name:")
    <br />
    @Html.EditorFor(b => b.Name, new { htmlAttributes = new { @class = "form-control" } })
    @Html.ValidationMessageFor(b => b.Name, "", new { @class = "text-danger" })
    <br />


    <button class="btn btn-primary" type="submit">Update</button>
}